import styled from "styled-components";

export const Stylediframe = styled.iframe`
  width: 100%;
  height: 100%;
  margin: auto;
  position: relative;
  resize: both;
  border: 0px;
  z-index: 100;
`
