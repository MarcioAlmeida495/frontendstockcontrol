import styled from "styled-components";

export const Stylediframe = styled.iframe`
  width: calc(100% - 100px);
  height: 100vh;
  margin: auto;
  position: absolute;
  resize: both;
  z-index: 100;
`
